server:
  port: 8888
spring:
   application:
     name: config_server
   cloud:
     config:
       server:
         git:
           uri: https://github.com/YuriLeao/maromba-config-service
           #username
           #passworld
           default-label: main
           search-paths:
           - 'usuario-service*'